#!/bin/bash
set -ueo pipefail


MODEL_URI="${PLATFORM_DOMAIN}/${COMPANY}/${MODEL}"
# PROVIDER_DIR="${SOURCE_DIR}/${PROVIDER}"
MODEL_DIR="${SOURCE_DIR}/${MODEL}"
CONVERTED_MODEL="${SOURCE_DIR}/${MODEL}/${MODEL}-${CONVERSION_TENSOR}.gguf"
QUANTIZED_MODEL="${SOURCE_DIR}/${MODEL}/${MODEL}-${QUANTIZATION}.gguf"
